# Backend Environment Variables Example
# ! DO NOT COMMIT THE ACTUAL .env FILE TO VERSION CONTROL
# This is a template. Real credentials should be stored in GitHub Secrets, Docker Secrets, or secure .env files ignored via .gitignore
# These will be injected via GitHub Actions secrets during CI/CD

# ---- PRODUCTION SSL ----
DB_SSL=false # false for staging, change to true and add below once you have the certificates
# SSL_CA=
# SSL_KEY=
# SSL_CERT=

# Node environment
NODE_ENV=development
# Can change to 'production' for production environment and alter according to needs

# Backend server port
PORT=3000

# SQL Database environment variables
MARIADB_DATABASE=databaseExampleName
MARIADB_USER=databaseExampleUser
MARIADB_PASSWORD=databaseExampleUserPassword
MARIADB_ROOT_PASSWORD=rootExamplePassword

# SQL Dialect
DB_DIALECT=MariaDB

# Docker SQL Database environment variables [DOCKER-DB LOCAL DEV]
DATABASE_URL=mariadb://databaseExampleUser:databaseExampleUserPassword@127.0.0.1:3307/databaseExampleName
MARIADB_HOST=localhost # set for docker databases and local development
MARIADB_PORT=3307 # set for docker databases and local development

# Super Admin information
SUPER_ADMIN=superAdminExampleUsername
SUPER_ADMIN_PASSWORD=superAdmin_ExamplePassword

# Admin information
ADMIN_EMAIL=adminExampleEmailAddress@example.com
ADMIN_USERNAME=adminExampleUsername
ADMIN_PASSWORD=adminExamplePassword!01

# Employee information
EMPLOYEE_EMAIL=employeeExampleEmailAddress@example.com
EMPLOYEE_USERNAME=employeeExampleUsername
EMPLOYEE_PASSWORD=employeeExamplePassword!01

# Database test users email & password
TEST_USER_PASSWORD=Passw0rd4312!Now
CLIENT_TEST_USER_EMAIL=test-client-email-address01@fortesting.com
EMPLOYEE_TEST_USER_EMAIL=test-employee-email-address01@fortesting.com
ADMIN_TEST_USER_EMAIL=test-admin-email-address01@fortesting.com

# NoSQL Database environment variables
MONGO_INITDB_ROOT_USERNAME=rootExampleUser
MONGO_INITDB_ROOT_PASSWORD=rootExamplePassword
MONGO_INITDB_DATABASE=exampleDB

MONGO_USERNAME=newExampleUser
MONGO_PASSWORD=newExamplePassword

## NEW USER Docker NoSQL Database environment variables [DOCKER-DB LOCAL DEV]
MONGO_URI=mongodb://newExampleUser:newExamplePassword@127.0.0.1:27018/exampleDB?authSource=exampleDB
MONGO_HOST=localhost # set for docker databases and local development
MONGO_PORT=27018 # set for docker databases and local development

# NODEMAILER configuration - gmail example
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_NAME=exampleUserFullName
SMTP_USER=exampleEmailAddress@gmail.com
SMTP_PASS=accountAppExamplePassword
SMTP_FROM=Cinephoria <exampleEmailAddress@gmail.com>

TEST_EMAIL_TO=exampleEmailAddress@gmail.com

# Nodemailer ethereal example - OPTIONAL
# SMTP_HOST=smtp.ethereal.email
# SMTP_PORT=587
# SMTP_NAME=exampleUserFullName
# SMTP_USER=exampleEmailAddress@example.com
# SMTP_PASS=accountExamplePassword
# SMTP_FROM=Cinephoria <info@ethereal.email>

# CORS  [LOCAL + DOCKER-DB]
CORS_ORIGIN=http://localhost:4200,http://localhost:3000

# Cookie secret
COOKIE_SECRET=exampleCookieSecretHere

# JWT
JWT_SECRET=exampleJwtSecretKey
JWT_EXPIRES_IN=1h

# Frontend URL
FRONTEND_URL=http://localhost:4200


## EARLIER CONFIGURATION ## Docker NoSQL Database environment variables [DOCKER-DB LOCAL DEV]
# MONGO_URI=mongodb://rootExampleUser:rootExamplePassword@127.0.0.1:27018/exampleDB?authSource=admin
# MONGO_HOST=localhost # set for docker databases and local development
# MONGO_PORT=27018 # set for docker databases and local development